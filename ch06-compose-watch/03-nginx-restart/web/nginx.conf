events {
  worker_connections 1024;
}

http {
  server {
    listen 80;
    server_name _;

    add_header X-App-Version "v2" always;

    location / {
      root /usr/share/nginx/html;
      index index.html;
    }
  }
}
