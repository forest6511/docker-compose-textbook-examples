name: ch06-go-rebuild

services:
  app:
    build:
      context: ./app
    ports:
      - "8080:8080"
    develop:
      watch:
        - action: rebuild
          path: ./app
          ignore:
            - "*.md"
            - ".git/"
